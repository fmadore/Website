---
target: /cv, /cv/timeline, CV components + the five roadmap "left for later" items (6.1)
p0_count: 0
p1_count: 5
timestamp: 2026-09-12T06-47-13Z
slug: src-routes-cv-page-svelte
---

# P — `/impeccable polish` — `/cv`, `/cv/timeline`, the CV components and the five named "left for later" items (roadmap 6.1)

Method: served production build on :4174, no rebuild, no repo edits. Playwright 1.62.1 (13 throwaway probes in the scratch dir): `/cv` computed styles for all 17 section heads, all 24 `h4`s and 49 sampled ledger rows; box-and-baseline geometry at 1440 / 768 / 375 / 360 / 320 and at 200 % zoom (720 CSS px @2×); character-level `Range` rect sweep for mid-word breaks on 14 routes at 320; `emulateMedia({media:'print'})` + four `page.pdf()` runs (A4, 12 mm margins) read back page by page; a per-section `content-visibility` experiment at 1× and 4× CPU throttle via CDP `Emulation.setCPUThrottlingRate`; axe-core 4.x under `wcag2a/2aa/21a/21aa/22aa` on `/cv`, `/cv/timeline` and a research project page × 1440/375; a 22-stop Tab walk; hover/focus/disabled/reduced-motion state reads; `npm run shot` captures at 1440 light, 1440 dark, 320 and 375 read back as PNGs. Themes swapped by toggling `html.dark` with transitions killed first. Bundled detector: **0 findings** on the slice's source files; on `build/cv.html` it reports 23 `tight-leading` hits that are all `clamp()` it cannot evaluate, and three `design-system-font` hits that are the metric-matched `local()` fallback faces added in 5.1 — **all false positives, verified in context**. The detector's **URL mode is degraded on this machine** (`puppeteer is required for URL scanning`), so every rendered measurement here is Playwright's, not the detector's.

## Evidence

**Geometry.** `/cv` at 1440: `.cv-container` 1152 px max-width, 32 px padding, 1 px `--color-border` box edge; 17 sections, 24 `h4` subheads, **219 ledger rows**, 116 links, 23,344 px tall (28,952 at 768; 49,146 at 375; 56,493 at 320). Same-role typography is **byte-identical across all 17 sections**: every `h3` `Archivo|33.744px|750|-0.33744px|35.4312px|"wdth" 112`, colour `rgb(25,21,9)`, margin `0 0 16px`; every `section` `border-top 3px rgb(25,21,9)` + `padding-top 12px` (`--rule-gap`) + `margin-top 48px`; every `h4` `Spline Sans Mono|11.1104px|600|1.33325px`, margin `24px 0 4px`. Ledger: one key voice (`Spline Sans Mono|13.3328px|500|0.8px|21.33px`, `rgb(107,99,78)`) plus the two sanctioned pine keys at weight 600; one content voice (`Newsreader|18px|28.8px`); `padding 10px/10px`, `border-top 1px rgb(220,212,190)`, `gap 8px/20px`, grid `104px 1fr` (`240px 1fr` on the wide-key skills rows).

**Baseline alignment.** Zero-height inline-block probes on the first row of each of the 17 sections: key-vs-content baseline delta **0.00 px in 15 of 17**. The two apparent `−26.8 px` outliers (`cv-education`, `cv-consulting`) are a probe artefact — those entries open with a bare text node, so the probe read the _second_ block child; the rect-bottom sweep over 49 rows gives a uniform −1 px, which is the Newsreader/Spline descender difference under correct baseline alignment.

**Theme parity.** Light → midnight, every token maps: `h3` `#191509 → #efe7d6`; section rule the same pair; row hairline `#dcd4be → #383126`; pine key `#1e6a56 → #4fbb99`; container edge `#c9c0aa → #453d2f`; grounds `#faf7ef → #171310`. The primary button inverts to a cream fill with dark text — correct, and it is the single brightest block on the midnight sheet.

**Print (Chrome, A4, 12 mm).** As shipped: **38 pages**, 886 KB. Dropping `page-break-inside: avoid` from `#cv-content section` alone: **33 pages**. Also hiding the skip link, site masthead and footer: **32 pages** (796 KB). Adding row-level `break-inside: avoid` back: 33 pages. Page 1 of the shipped PDF carries the site masthead with a hamburger glyph and then 45 % blank paper; **every** page carries an ink-filled `SKIP TO CONTENT` stamp at bottom-left. In print media: body `#fff` / `#191509` ✓, container padding 40 px and border 0 ✓, section rule 3 px ink ✓, row hairline 1 px ✓, links ink + underline ✓, `http` hrefs appended ✓, `.cv-toc` and `.cv-actions` `display:none` ✓, `header` and `footer` `display:block` ✗.

**320 px.** `.cv-main-title` "Curriculum" needs **235.0 px**; the title box is **222 px** — deficit **13 px**, so `overflow-wrap` breaks it `Curriculu | m Vitae`. Gutter chain: 320 → `.container px-4` 16 px each side (288) → `.cv-container` 1 px border + 32 px padding each side (222). **96 px of gutter + 2 px of border, exactly as the roadmap recorded.** Break window measured at 8 px steps: fails at 320 and 328, fits from **336** (238 avail vs 235.6 needed). Candidate fixes at 320, measured: container padding 32→24 px = 238 avail ✓; 32→20 px = 246 ✓; 32→16 px = 254 ✓; title 37.5→32 px = 200.5 needed in 222 ✓; outer `.container` 16→8 px = 238 ✓. No horizontal overflow at any width, and none at 200 % zoom (720 CSS px: `documentElement.scrollWidth − innerWidth = 0`, 0 offenders, row grid `104px 418px`).

**content-visibility experiment** (per-section `content-visibility:auto; contain-intrinsic-size:auto <measured>px`, injected at 1440): document height 23,344 → **22,704 px (−640, −2.7 %)**; theme-swap style+layout median **79.9 → 41.8 ms at 4× CPU** (−48 %), **15.5 → 10.0 ms unthrottled** (−35 %); every one of the 17 contents anchors lands **16 px** off its un-contained position (11 px on the last), well inside the 80 px `scroll-margin-top`; the 17 section offsets shift by −16 … −640 px cumulatively before the sections render. This is materially better than the 5.1 note (which used a single `auto 800px` estimate and collapsed the document to 7,764 px) — the difference is entirely the per-section measured intrinsic size.

**Accessibility.** axe-core WCAG 2.2 AA: **0 violations** on `/cv`, `/cv/timeline` and `/research/religious-activism-campuses-togo-benin`, at both 1440 and 375. Of the 49 sub-24 px links on `/cv` at 375, **all 49 are inline in a sentence** (2.5.8 inline exception) — verified by testing each anchor's parent for surrounding text nodes. The masthead costs 23 tab stops before the sheet; focus rings are `2px solid rgb(30,106,86)` with an offset on every control checked. Hover/focus/disabled/reduced-motion all present and correct (see Clean).

**Research project panels.** `/research/religious-activism-campuses-togo-benin` at 1440: 9 `.relevant-item` boxes, 277–306 px each, **2,606 px total**, in a 580 px column; `--color-surface` fill, 1 px `--color-border`, 24 px padding, 16 px gaps, pine border on hover. At 375: 298–367 px each, **3,068 px**, first card at y = 3,814. The same record as a `BibliographyRow` on `/publications` is **147 px** (1,120 px column) while carrying _more_ fields (kind, open-access note, cover plate, byline, DOI action, Cite). The `Funding` panel on the same page is already a ledger (one row, 172 px), and `LatestActivities` on the home page is already `<ol class="ledger log-ledger">` with `.ledger-row`/`.ledger-key` and the date as the hanging key — rows 84–132 px in a 352 px column, whole row is the anchor, **0 child links**.

---

## Findings

### The five named "left for later" items

- **[NAMED-1 / P2] The CV title breaks mid-word at 320; the gutter is the cause, and the narrowest fix is a phone step on the sheet's padding.** `src/lib/components/cv/CVHeader.svelte:215–237` + `src/routes/cv/+page.svelte:122–128`. Measured at 320: "Curriculum" needs **235.0 px**, the title box gives **222 px** (viewport 320 − 32 px outer `.container` − 64 px `.cv-container` padding − 2 px border = 222). Fails at ≤ 331 px only; fits from 336. **Fix:** add `@custom-media --2xs-down (max-width: 359px);` to `src/styles/base/media.css` beside `--xs-down` (line 14), then one rule in `src/routes/cv/+page.svelte`'s style block: `@media (--2xs-down) { .cv-container { padding: var(--space-5); } }` → 246 px available, an 11 px margin over the word, and +16 px of measure on all 219 ledger rows at that width. Do **not** reuse `--xs-down` (≤ 479 px): that would change 375 and 440, which are signed off. Do **not** take the type step — the roadmap's own diagnosis (gutter, not type) is confirmed by measurement, and the 2xl step would drop the masthead to 30 px, two tiers below every other index title.
  Other routes whose `h1` breaks mid-word at 320 (character-level Range sweep over 14 routes; these are **not** gutter problems — they sit in the page default 16 px gutter, so they need a type step and belong to their own agents): `/publications` "Publications" needs 272.3 px in 256 (deficit **16.3**), breaks `cation|s` with an orphaned "s"; `/publications/visualisations` "Visualisations" needs 299.4 px in 264 (deficit **35.4**), breaks `isatio|ns`; `/` nameplate "Frédérick Madore, PhD" renders **4 lines** at 40 px with a break inside `rédéri|ck`. `/cv/timeline`, `/conference-activity`, `/activities`, `/research`, `/teaching`, `/digital-humanities`, `/style-guide`, `/conference-activity/slides` and the longest research project title are all clean at 320.

- **[NAMED-2 / P2] `RelevantItemsList` is the last card grid of dated records; convert it to the `.ledger` idiom `LatestActivities` already uses.** `src/lib/components/panels/RelevantItemsList.svelte:72–78`, `src/lib/components/panels/RelevantItemCard.svelte:24–55`, `src/styles/components/panels.css:68–77`. Measured: 9 cards, **2,606 px** at 1440 / **3,068 px** at 375, against **147 px** for the same record as a `BibliographyRow` and 84–132 px for a `LatestActivities` ledger row. Every card is a dated, keyed record with the date already printed (`2025`, `6 November 2025`) — the Ledger Default Rule applies and no card reason is documented anywhere in the component.
  **Reuse `.ledger` / `.ledger-row`, not `BibliographyRow`.** `BibliographyRow` is the four-column finding-aid entry (year | cover | body | actions) with plate, `Cite`, `citedCount` and kind-notes; a project panel has no plate column and no citation controls, and adopting it would drag `bibliography.css` into the research route. Follow `LatestActivities.svelte:65–86` instead — the precedent is on the same system, two panels away.
  **Conversion spec:** `<ul class="item-list">` → `<ol class="ledger ledger--ruled">`; each `<li>` holds a single `<a class="ledger-row">` wrapping the whole record (so the row is the one link). **Hanging key = the record's date** (`item.date`), with the formatted type as a `.ledger-status` beneath it — exactly the `formatDayMonth` + year pairing in `log-ledger`. Content column: `.ledger-content` > `.ledger-title` (the serif title, already the right voice), `.ledger-meta` for the byline, `.ledger-desc` for the abstract excerpt. **Delete:** `.relevant-item` box (padding, `--color-surface` fill, `--color-border` edge, pine hover border), `.relevant-item-meta` and its hairline (the row's own `border-top` is the hairline — drawing both is a Hairline Pairing crossing), `.relevant-item-action` / `.relevant-item-link` and the `View record →` anchor (see NAMED-2b below). `RelevantItemCard.svelte` becomes unnecessary; if it is kept as a row component, rename it. In `panels.css`, `.item-list` (line 68) loses its flex/gap rules to `.ledger`; `.activities-list` in the same selector list has **no consumer left** (`LatestActivities` uses `.ledger log-ledger`) and goes with it.
  **`src/app.css` does not import `panels.css`** — it names it only in the comment block at lines 24–30 explaining that page-specific CSS is imported by the component that owns it. That comment must be updated in the same commit if `panels.css` shrinks or goes.

- **[NAMED-3 / P2] `.guide-note` can leave the base sheet, but the swap is not `.prose` on the same element.** `src/styles/base/typography.css:203, 225, 245`; `src/routes/style-guide/+page.svelte` (**52 rendered instances**; 47 `class="guide-note"`, 3 `guide-note guide-specimen-prose`, plus repeats inside an `{#each}`) with the class's own rule at `+page.svelte:1969–1976`. `.guide-note` styles font-family, **font-size `--font-size-base`**, line-height, **colour `--color-text-soft`**, `max-width: --measure-prose` and `margin: 0 0 var(--space-lg)` — so a naked class swap changes: font-size **18 px → 21.6 px**, colour **`rgb(92,84,66)` → `rgb(25,21,9)`**, max-width **495 px → 598 px**, margin-bottom **24 px → 16 px** (because `.prose p` needs the paragraph to be a _descendant_ of `.prose`, and here it would be the `.prose` element itself). **Only 2 of the 52 notes contain an `<a>` at all**, and both are in the prose-link specimen at `+page.svelte:851–859` — so the class's membership in the base sheet's opt-in list buys exactly one underlined link site-wide, the guide's own demonstration of the idiom. **Fix (narrowest):** add `class="prose guide-note"` to those two specimen paragraphs only (the component-scoped `.guide-note` at 0,2,0 still beats `.prose` at 0,1,0 on every declaration, so nothing moves optically — verified against the measured values above), then delete `.guide-note` from the three selector lists in `typography.css`. **Also required in the same commit:** `+page.svelte:834` prints the opt-in list as reader-facing copy and names `.guide-note` as a member; that sentence goes stale.

- **[NAMED-4 / P3] `--color-citation` has zero live consumers and ships twice per route; retire it.** Declared at `src/styles/base/variables.css:139` and `src/styles/base/dark.css:96`. A grep over `src/`, `mcp/`, `scripts/`, `tests-e2e/`, `DESIGN.md` and `/style-guide` returns **only those two declarations plus one explanatory comment** at `src/styles/base/typography.css:503`. No test reads it (`designTokenParity.test.ts` does not). It is in the shipped CSS: 2 occurrences in `build/app/immutable/assets/0.*.css` and 2 in the inlined `build/cv.html`. **Fix:** delete `variables.css:134–139` (comment block and declaration) and `dark.css:92–96`; edit the `.citation` comment at `typography.css:503–506` so it no longer says "The token stays declared for a consumer that wants the accent by that name" — the note should simply record that an inline citation is ink text with a pine underline and pine on hover. Nothing else references it, so no DESIGN.md or style-guide change is needed.

- **[NAMED-5 / P3] `content-visibility` on the CV: do not ship.** Measured on the served build with per-section `contain-intrinsic-size: auto <measured>px` at 1440. **Gain:** theme-swap style+layout median **79.9 → 41.8 ms at 4× CPU** (−48 %), **15.5 → 10.0 ms unthrottled** (−35 %). **Cost:** the document loses **640 px** and the 17 section offsets drift by −16 … −640 px until each section renders, so the scrollbar lies while the reader scrolls; the 17 contents anchors each land **16 px** off (harmless — inside the existing 80 px `scroll-margin-top`), which corrects the 5.1 note's "collapsed the ToC anchors", but only because the intrinsic sizes were measured per section rather than guessed. **That measurement is the problem:** 17 hard-coded pixel heights are content-coupled (the CV grows with every publication) and viewport-coupled — the document is 23,344 px at 1440 and **49,146 px at 375**, so a 1440-measured estimate is roughly half the truth on a phone, which is where the 4× throttle scenario actually lives. Against that, the saving is 38 ms, once per session, on a deliberate theme toggle already under the 100 ms threshold. `src/lib/utils/pdfCvGenerator.ts` is safe either way (it reads `textContent` through `querySelector`, never geometry — checked lines 92–446), but any future attempt must also scope the declaration to `@media screen`, since the printed CV is this page's product. **Recommendation: do not ship; record the numbers and close the item.**

### Other findings, in triage order

- **[P1] Escape from the contents panel drops focus to `<body>`.** `src/lib/components/cv/CVTableOfContents.svelte:130–134`. Measured: open the panel, Tab into it (focus lands on `.cv-toc-link` ✓), press Escape → panel closes and `document.activeElement` is **`BODY`**. The reader is returned to the top of the tab order, 23 masthead stops away from where they were. WCAG 2.4.3. **Fix:** `bind:this` the FAB button, and in `handleKeydown` call `fabEl?.focus()` after `isOpen = false` — only when the panel was open, so Escape elsewhere on the page is unaffected.

- **[P1] The contents control is tab stop 128 of 142 — after every link in the CV.** `src/routes/cv/+page.svelte:114` renders `<CVTableOfContents />` after `<div id="cv-content">`. Measured: index 127 of 142 focusable elements, preceded by the last "Verified on Web of Science" and "Listen ↗" links. A floating table of contents whose entire purpose is to skip a 23,344 px document is the second-to-last thing a keyboard user can reach. **Fix:** move the `<CVTableOfContents />` call above `<div id="cv-content">` (line 49). The FAB is `position: fixed`, so nothing moves visually; it becomes roughly tab stop 24, immediately after the masthead.

- **[P1] The skip link prints as a solid ink stamp on every page of the printed CV.** `src/routes/+layout.svelte:112–129`. `.skip-link` is `position: fixed; top: 0; transform: translateY(calc(-100% - var(--space-2)))`. Chrome repeats fixed elements on every page of a paged layout, and the transform places it inside the page box: the A4 PDF carries `SKIP TO CONTENT` as an ink block at bottom-left of **all 38 pages** (read back from the PDF). **Fix:** one rule in the existing global print block at `src/styles/base/typography.css:587` — `.skip-link { display: none !important; }`. This is a site-wide defect, not a CV one; the global sheet is the narrowest correct level.

- **[P1] The site masthead and footer print on the CV.** `src/routes/cv/+page.svelte:273–352` hides `.cv-actions` and `CVTableOfContents.svelte:398–402` hides `.cv-toc`, but nothing hides the site chrome: in print emulation `header` and `footer` both compute `display: block`, and page 1 of the PDF opens with the "FRÉDÉRICK MADORE" nameplate bar and a hamburger glyph — a navigation affordance on paper. **Fix:** in the CV's own `@media print` block, `:global(.site-header), :global(.site-footer) { display: none !important; }`. Removing the chrome plus the skip link took the PDF from 33 to **32 pages**.

- **[P1] `page-break-inside: avoid` on whole sections costs five pages of blank paper.** `src/routes/cv/+page.svelte:321–323`. Measured on the same document: **38 pages** as shipped, **33** with that one declaration neutralised. The rule is applied to `#cv-content section`, but a CV section is not a page-sized unit — Publications alone runs many pages, so Chrome ignores the rule there, while short sections (Awards, Languages, Affiliations, Computer Skills, Consulting) each get pushed to a fresh page; page 1 of the shipped PDF ends 45 % of the way down and page 2 at 40 %. **Fix:** drop the section-level rule and keep breaking at the row: `#cv-content .cv-entry { break-inside: avoid; }` and `#cv-content h4 { break-after: avoid; }` alongside the existing `h3 { page-break-after: avoid }`. Measured at 33 pages with chrome still printing, i.e. the row-level guard costs nothing.

- **[P2] The contents entries' accessible names read "Publications 42entries".** `src/lib/components/cv/CVTableOfContents.svelte:220–222`. The template writes `{count}<span class="sr-only"> {…}</span>`, but Svelte trims the leading whitespace at compile time, so the DOM is `<span class="cv-toc-count">5<span class="sr-only">entries</span></span>` and the button's text is `"Professional Appointments 5entries"` — measured on all 17 entries. The e2e suite locates by accessible name. **Fix:** move the space inside the expression — `<span class="sr-only">{count === 1 ? ' entry' : ' entries'}</span>` — or use `&nbsp;`.

- **[P2] Contents rows are 23 px tall with a pointer, 1 px under the 2.5.8 minimum.** `src/lib/components/cv/CVTableOfContents.svelte:332` (`padding: var(--space-1) var(--space-2)` on an 11.1 px mono line) with the 44 px `min-height` scoped to `@media (--touch)` at line 390–395. Measured at 1440: 12 of the 17 rows are **23 px**; the five that wrap are 38 px. WCAG 2.5.8 applies to all pointer inputs, not only touch. **Fix:** give `.cv-toc-link` a base `min-height: var(--space-6)` (24 px) and keep the `--touch` step at 44 px.

- **[P2] `View record →` is a 91 × 16 px duplicate link, nine per research project page.** `src/lib/components/panels/RelevantItemCard.svelte:51–54`. Every card already links the record from its title (`links: 2` per card, both to the same href), and the second is a standalone action — not inline in a sentence, so the 2.5.8 inline exception does not apply. Measured at 375: **16 px tall, ×9**. This is the same call the 4.1 session already made on `BibliographyRow` ("a third link to the same page is only noise in the tab order"). **Fix:** it disappears in the NAMED-2 ledger conversion, where the row itself is the anchor; if the conversion is deferred, delete the action block and leave the title as the one link.

- **[P2] `RelevantCommunications` hand-rolls a sixth communication-type register and it is missing `podcast`.** `src/lib/components/panels/RelevantCommunications.svelte:148–159`. `typeUtils.ts` holds five registers (`COMMUNICATION_TYPE_BADGE/SEO/LIST/CHIP/CHART_LABELS`, lines 16–71), each covering all eight corpus types and each guarded by the registry test the 4.1 session added. The local `typeMap` duplicates `BADGE` except `workshop: 'Workshop'` vs `'Workshop Presentation'`, **omits `podcast` entirely**, and escapes the test — so a podcast talk filed under a project prints the raw key `podcast` in its kind eyebrow. This is exactly the `conference-proceedings` bug 4.1 fixed elsewhere. **Fix:** delete the local map and import `COMMUNICATION_TYPE_BADGE_LABELS`, or add a `COMMUNICATION_TYPE_PANEL_LABELS` beside the other five in `typeUtils.ts` so the registry test covers it — mirroring `RelevantPublications.svelte:6`, which already imports `PUBLICATION_TYPE_PANEL_LABELS`.

- **[P2] The card truncates with `'...'` at an undocumented second constant.** `src/lib/components/panels/RelevantItemCard.svelte:46`: `item.abstract.substring(0, 120) + '...'`. Three ASCII dots, visible in the 1440 capture on all three publication cards ("d'Abome...", "organize...", "au T..."). DESIGN.md's Punctuation Rule is `…` never `...`, and the 3.2 session swept the site for exactly this. Worse, the value arriving here is already `abstractExcerpt`, cut at `ABSTRACT_EXCERPT_LENGTH = 220` (`src/lib/data/publications/summaryConfig.ts:18`); the component then cuts it again at a bare literal 120. **Fix:** use `…`, and name the panel length as an exported constant in `summaryConfig.ts` beside `ABSTRACT_EXCERPT_LENGTH` so the two truncations are one decision. (A word-boundary cut would also stop "d'Abome…" — optional.)

- **[P2] Dead lazy-load machinery in the contents component.** `src/lib/components/cv/CVTableOfContents.svelte:66–70` (a `MutationObserver` on `{childList: true, subtree: true}` over `#cv-content`), `:101–107` (a 500 ms `setInterval` poll) and `:202–204` (`Loading remaining sections…`). All seventeen sections have been static imports since 5.1 (`src/routes/cv/+page.svelte:12–32`), so `scanSections()` in `onMount` finds all 17 on its first call. Measured on the served build: opening the panel prints **17 entries and no note** — `panel.querySelector('.dateline')` is `null`. The observer still fires through hydration on a 219-row document. **Fix:** delete the note, the `MutationObserver` and the `setInterval`; keep `scanSections()` and the `IntersectionObserver` scroll-spy. If the note is kept as insurance, it must first be reachable — at present it is unreachable code.

- **[P2] The research panel's type filters are `Button`s where the system owns a chip.** `src/lib/components/panels/RelevantPublications.svelte:66–78` + `src/styles/components/panels.css:126–167`. Measured: "BOOK" 61 × **23 px**, "BOOK CHAPTER" 132 × 23 px at 1440; 22 px at 375 — under the 24 px minimum in both. They are facet controls doing exactly what `.chip` / `.chip--selected` (`ink-signal.css:649–715`) does everywhere else on the site, minus the appended count DESIGN.md's chip spec calls for. **Fix:** render them as `.chip` with `.chip--selected` for the active type and the record count appended, and drop `.filter-button` (`panels.css:136–167`) with them.

- **[P2] Three icon families and two fill models in one control row.** `src/routes/cv/+page.svelte:53` uses `lucide:trending-up` (stroked, `stroke-width: 2`, 20 × 20) directly beside `src/lib/components/cv/PdfGenerator.svelte:47,52` `mdi:file-pdf-box` (solid fill, 20 × 20); `CVHeader.svelte:156,176` adds four more `mdi:*` at 16 × 16 and the `academicons` ORCID mark at 16 × 16 on a `0 0 512 512` box, while the 30 DOI marks render at **14 × 14**. Census of all 45 SVGs on `/cv`: 2 stroked (one lucide, one hand-rolled in the FAB at `stroke-width: 2`, 22 × 22), 43 filled; sizes 14, 16, 20, 22. **Fix (narrowest):** swap `lucide:trending-up` for its MDI equivalent so the two buttons in the actions row share a family and a fill model, and set the DOI mark to 16 px like every other inline mark. The FAB's hand-rolled SVG is a deliberate menu/close pair and can stay.

- **[P3] `.cv-actions` carries an 8 px bottom padding that is off the rule-gap system.** `src/routes/cv/+page.svelte:141` — `padding-bottom: var(--space-2)`. Measured: 8 px between the button row and the `.cv-header` 4 px masthead rule, where every other ruled module on the page opens on `--rule-gap` (12 px) and the header's own interval below the rule is 24 px. No comment explains the 8. **Fix:** either `var(--space-sm)` (12 px, matching `--rule-gap`) or drop it and let `.cv-header`'s own `margin` carry the interval.

- **[P3] On a phone the primary action sits below the secondary.** `src/routes/cv/+page.svelte:52–57`. At 375 and 320 `.cv-actions` wraps: Timeline at y = 129, **Download PDF at y = 191**. DOM order puts the outline link before the primary button; at 1440 `justify-content: flex-end` makes the primary rightmost, which reads correctly, but the stacked phone order inverts it. **Fix:** reverse the DOM order (`<PdfGenerator />` first) and add `flex-direction: row-reverse` inside the existing `@media (--md)` block at line 144 so the desktop order is preserved.

- **[P3] Em dash in the CV standfirst.** `src/lib/components/cv/CVHeader.svelte:25` — `{author.fullName} — {author.position}`, rendering "Frédérick Madore, PhD — Historian | Digital Humanities & AI | …". DESIGN.md's Punctuation Rule (line 19 of § Voice & Copy) says no em dashes in interface copy; a colon reads best here given the commas already in the string. **Caveat for the fix agent:** em dashes are used in visible copy elsewhere on the site as a _structural_ separator, and one of those forms — `Fig. N — …` — is documented in CLAUDE.md as the plate-caption idiom. So this is one instance to change, not a site-wide sweep, and if the owner reads a standfirst separator as structural too, decline it and record that instead.

- **[P3] `/cv/timeline` hides two thirds of the chart at 375 with no affordance.** `src/routes/cv/timeline/+page.svelte:51–64`. Measured at 375: the SVG is **776 px** (`viewBox="0 0 776 412"`, `width="776"`) inside a 263 px `.timeline-container` — **457 px, 59 %, off-screen**; the screenshot shows the axis reaching only 2010. The container does scroll (`overflow-x: auto`, `scrollWidth 776 / clientWidth 263`) and axe raises nothing (the chart's records are focusable, so 2.1.1 is satisfied), but nothing on the page says the plate scrolls sideways — the standfirst names the arrow keys and stops there. **Fix:** add the horizontal scroll to the standfirst sentence at line 37–39 under `--sm-down`, or give `.timeline-container` a `role="region"` with an `aria-label` and a mono caption in the `.state-note` register saying the plate scrolls.

- **[P3] The CV and its timeline disagree about how many publications there are.** The contents ledger prints `Publications 42 entries` (counted from the rendered `.cv-entry` rows, `CVTableOfContents.svelte:60`) while `/cv/timeline` prints `PUBLICATIONS 44 records` (counted from `getAllTimelineItems()`). Two counts of the same corpus, one click apart. **Fix:** decide which set is the record (the timeline probably includes items the CV section does not list) and either reconcile the two sources or make the two labels name different things.

- **[P3] `.space-y-3` is applied to every CV ledger and then cancelled.** `src/lib/components/cv/CVSection.svelte:113` and the hand-rolled sections put `.space-y-3` on every ledger; `src/routes/cv/+page.svelte:215–217` then sets `margin-top: 0` on `.ledger.space-y-3 > * + *` to undo it. The class is load-bearing — `src/lib/utils/pdfCvGenerator.ts:407` uses it as a negative selector — so it cannot simply be deleted. **Fix:** rename the PDF hook to a data attribute (`data-cv-ledger`) in the components and in `pdfCvGenerator.ts`, then drop both the utility class and the override, so nothing is applied only to be cancelled. Also drop the now-unreferenced `.activities-list` arm at `src/styles/components/panels.css:68`.

- **[P3] `RelevantCommunications` carries filter state it can never change.** `src/lib/components/panels/RelevantCommunications.svelte:119` (`let selectedType = $state<string|null>(null)`) and `:141–145` (`filteredList`). No filter snippet is passed to `RelevantItemsList`, so `filteredList` is always `communicationList`. **Fix:** delete both and pass `communicationList` directly — or give talks the same type filters publications have, if that is the intent.

- **[P3] Two spacing values on the CV are outside DESIGN.md's documented scale.** `--space-2-5` (10 px) is the `.ledger--tight` row padding (`ink-signal.css:229`) and therefore the most-repeated spacing value on the page — **219 rows × 20 px** — and `--space-1-5` (6 px) is the `h4` underline gap (`src/routes/cv/+page.svelte:202`). Both are real tokens on the numeric ladder in `variables.css` (lines 378–380), but DESIGN.md's `spacing` block lists only 2/4/8/12/16/24/32/48/64/96. **Fix:** this is documentation drift, not implementation drift — the tight row's density is argued in the idiom's own comment. Add the two half-steps to DESIGN.md's spacing block with a one-line note that they exist for the tight ledger and the label rule, rather than changing either value.

## Declined

- **"Use more whitespace" / "loosen the 10 px ledger padding"** (polish playbook, Layout and type: "separate distinct groups generously"). Declined: density is a stated value in this system (DESIGN.md Peer-respecting density; the `.ledger--tight` comment prices the alternative at "roughly a screen and a half of added paper carrying no information" across 219 rows). The brief's guardrail names "more whitespace" explicitly.
- **"Reduce the `h1`/`h3` sizes so nothing wraps at 320"** (playbook: "test measure, wrapping … and zoom"). Declined for the CV: the gutter is the measured cause (96 px of 320), not the type, and dropping the title two tiers would break the 4 px-masthead → 3 px-section → 1 px-entry hierarchy the sheet is built on. Reported as a gutter fix instead.
- **"Add a loading state / skeleton while the CV sections mount"** (playbook: "missing loading … states"). Declined: all seventeen sections are in the prerendered HTML by owner's ruling in 5.1. The correct move is the opposite — remove the leftover `Loading remaining sections…` (P2 above).
- **"Animate the contents panel open"** and **"add a scroll-progress indicator to the 23,344 px sheet"**. Declined: the register is print, motion is near zero by design, and the roadmap excludes `animate`/`delight` from this project outright.
- **"Give the research cards a shadow or a hover lift to separate them from the page"**. Declined twice over: the Drawn Depth Rule, and because the correct answer is to stop drawing boxes at all (NAMED-2).
- **"Make `/cv/timeline`'s `h1` an index masthead like `/research` and `/teaching`"**. Investigated and declined on measurement: `/cv/timeline` is at the **record tier** (`52.736px / 820 / .page-title`, no rule), which is exactly what `/publications/[id]` (`record-title`) and `/publications/visualisations` (`page-title`) render. It is in step with its tier; the 4.3 fix applied to two index pages, and this is not one. (One genuine cross-slice oddity noticed in passing, for whichever agent owns it: `/conference-activity/slides` opens at the **index** tier, 72 px under 4 px, while its sibling `/publications/visualisations` opens at the record tier — two sub-pages of two indexes disagreeing.)
- **The detector's 23 `tight-leading` findings and three `design-system-font` findings on `build/cv.html`**. Declined as false positives after checking each in the rendered page: the leading ratios come from the detector dividing a `line-height` by a `clamp()` it cannot evaluate (the real computed leading on the CV is 28.8 px on 18 px body and 35.4 px on 33.7 px heads), and the three "undeclared" families are the `*-Fallback` metric-matched `local()` faces added in 5.1. The detector's URL mode is unavailable on this machine (no puppeteer), so it contributed nothing beyond the file scan, which was clean.

## Clean

Checked and found correct, with the measurement:

- **Same-role typography across all 17 sections.** Every `h3` identical on family, size, weight, tracking, line-height, `wdth` axis, colour and margin; every section rule `3px` ink at `--rule-gap` 12 px and `--space-2xl` 48 px above; all 24 `h4`s identical. Zero drift section to section — the thing the brief asked to be compared hardest.
- **Ledger key/content baseline alignment.** 0.00 px delta on the first row of 15 of 17 sections by zero-height inline-block probe (the two outliers are a probe artefact, diagnosed above); a uniform −1 px rect-bottom delta across 49 sampled rows, which is the correct descender difference between Spline Sans Mono and Newsreader under `align-items: baseline`.
- **4 px rhythm.** Every spacing value on the sheet is on the grid — container 32/24/48, actions gap 16, header rule 4 + gap 12 + margin 24, dateline and title margins 12, section 48/12, `h3` 16, `h4` 24/4, ledger gap 8/20, contact 24/24 — with the two documented half-steps (10 px, 6 px) noted as a P3 documentation item, not a spacing error.
- **Both themes.** All seven measured token pairs map correctly light → midnight; the primary button inverts to a cream fill with dark text; no hard-coded colour anywhere in the CV components.
- **200 % zoom** (720 CSS px @2×): `scrollWidth − innerWidth = 0`, zero elements past the viewport, ledger grid still two columns at `104px 418px`.
- **No horizontal overflow** at 1440, 768, 375, 360 or 320 on `/cv`, and none on `/cv/timeline`.
- **axe-core WCAG 2.2 AA: 0 violations** on `/cv`, `/cv/timeline` and `/research/religious-activism-campuses-togo-benin`, at 1440 and 375.
- **WCAG 2.5.8 on the CV sheet.** All 49 sub-24 px links at 375 are inline in running text (each tested for surrounding text nodes) — the inline exception applies to every one; 0 standalone undersized targets inside `#cv-content`. The contact stack's 24 px `min-height` (`CVHeader.svelte:294–298`) still holds.
- **Interaction states.** `.doi-link` `rgb(92,84,66)` no underline → pine + underline on hover; `.review-link` ink → pine + underline; `.btn-secondary` transparent/`#a39b86` → `#ece4d1` ground, pine label, ink edge; `.btn-primary` and the FAB `#191509 → #0e0b04`; disabled PDF button `opacity 0.5` + `cursor: not-allowed`; focus rings `2px solid rgb(30,106,86)` with offset on every control in the 22-stop Tab walk; `prefers-reduced-motion` kills every animation and transition on the container, the FAB and the spinner.
- **PDF export states.** Rest / `Generating…` / `Download failed. Try again.` all present with the glossary's exact strings; the failed label grows the button 197 → 355 px, absorbed by `justify-content: flex-end` at 1440 and by the stack at 375.
- **Contents anchors.** Clicking a contents entry lands the section 80–85 px from the top against a 76 px sticky header — `scroll-margin-top: calc(var(--space-16) + var(--space-4))` clears it with 4–9 px to spare, verified on three sections. `scrollIntoView` honours `prefers-reduced-motion` (`CVTableOfContents.svelte:123–126`).
- **Contents panel.** Correct `aria-expanded`, `aria-label`, `aria-current`; opening leaves focus on the FAB and Tab enters the list; 17 entries with live row counts summing to the 219 rows on the page; `max-height: 60vh` + `overflow-y: auto` contains it at both 1440 (555/538) and 375 (821/485); 44 px rows under `--touch`.
- **Print ink.** In print media the sheet sets ink on stock rather than black on white (`body` `#191509` on `#fff`, `h3` `#191509`), keeps the 3 px section rules and 1 px row hairlines, appends `(href)` after external links while exempting `.doi-link` and `.link-url`, and hides `.cv-actions` and `.cv-toc`. The pine "current" key prints pine, which reads correctly on paper. All of that is right; only the chrome and the page-breaking are not.
- **`pdfCvGenerator.ts` reads no geometry** — `textContent` through `querySelector` only (lines 92–446) — so nothing in this slice's proposed fixes can break the PDF export.
- **Probe artefact, not a defect, for the record:** `http://localhost:4174/cv/` (with a trailing slash) fails to hydrate — `serve` maps it to `build/cv.html` while the page's relative asset paths resolve against `/cv/`, giving nine 404s and `Failed to fetch dynamically imported module`. `build/` contains `cv.html`, not `cv/index.html`, and the sitemap advertises `/cv`, so GitHub Pages returns `404.html` for `/cv/` rather than a broken page. Every measurement above was taken at `/cv`. `/cv` and `/cv/timeline` log **0 console errors** at 1440 and 375.
